
  CREATE TABLE "LEO"."VOOS" 
   (	"VOO_ID" NUMBER(*,0), 
	"DATA" DATE, 
	"TRECHO_ID" NUMBER(*,0), 
	"HORARIO_PARTIDA" TIMESTAMP (6), 
	"HORARIO_CHEGADA" TIMESTAMP (6), 
	"AEROPORTO_SAIDA_ID" VARCHAR2(3 BYTE), 
	"AEROPORTO_CHEGADA_ID" VARCHAR2(3 BYTE), 
	"VALOR_ASSENTO" NUMBER(10,2), 
	 PRIMARY KEY ("VOO_ID")
  USING INDEX PCTFREE 10 INITRANS 2 MAXTRANS 255 
  TABLESPACE "USERS"  ENABLE, 
	 FOREIGN KEY ("TRECHO_ID")
	  REFERENCES "LEO"."TRECHOS" ("TRECHO_ID") ENABLE, 
	 FOREIGN KEY ("AEROPORTO_SAIDA_ID")
	  REFERENCES "LEO"."AEROPORTOS" ("CODIGO_AEROPORTO") ENABLE, 
	 FOREIGN KEY ("AEROPORTO_CHEGADA_ID")
	  REFERENCES "LEO"."AEROPORTOS" ("CODIGO_AEROPORTO") ENABLE
   ) SEGMENT CREATION DEFERRED 
  PCTFREE 10 PCTUSED 40 INITRANS 1 MAXTRANS 255 
 NOCOMPRESS LOGGING
  TABLESPACE "USERS" ;
